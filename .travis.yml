language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest
notifications:
  email:
    on_success: never
    on_failure: change
after_success:
  - coveralls
